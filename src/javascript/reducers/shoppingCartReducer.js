const INITIAL_STATE = [];

export default function shoppingCartReducer(state = INITIAL_STATE, action) {
  switch (action) {
    case "ADD_ITEM" : {
      return state
    }
    default : {
      return state
    }
  }
}
